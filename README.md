# Project-E
run\
`python -m venv .venv && .venv\Scripts\activate.bat`\
`pip install -r .\requirement.txt` \
now in the same cmd
`python server.py`\
then you can run multiple instances of client code "opening in another new terminal"\
`python client.py`\
use authenticator app to signup and login
